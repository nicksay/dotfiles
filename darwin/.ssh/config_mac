Host *
  # Ignore Keychain errors if unsupported
  IgnoreUnknown UseKeychain
  # But don't use Keychain...
  UseKeychain no
  # .. and use 1Password for SSH keys
  IdentityAgent "~/.1password/agent.sock"
